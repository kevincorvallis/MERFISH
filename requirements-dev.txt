# Slim deps for offline pytest (pytest -m "not live") and core scripts.
# Python >=3.12 required (scanpy 1.12+). Install optional extras separately — see README.
#
# Pin exact versions for reproducible CI; bump with: uv pip install --upgrade -r requirements-dev.txt

pytest==9.1.1
numpy==2.4.6
pandas==2.3.3
scipy==1.18.0
scikit-learn==1.9.0
scanpy==1.12.1
anndata==0.12.17

# Needed by Scanpy Leiden + live tests (install with core env)
leidenalg==0.12.0
python-igraph==1.0.0

# Live tests + figure scripts (same env as core; STalign stays in .venv-reg)
squidpy==1.8.2
seaborn==0.13.2
matplotlib==3.11.0
brainglobe-atlasapi==2.3.1

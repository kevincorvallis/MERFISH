# Slim deps for offline pytest (pytest -m "not live") and core scripts.
# Install optional extras separately — see README "Optional extras".

pytest>=7.0
numpy>=1.24
pandas>=1.5
scipy>=1.10
scikit-learn>=1.2
scanpy>=1.9
anndata>=0.9
